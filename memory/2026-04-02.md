# 2026-04-02

- Worked on `micro-office` in `/home/user/.openclaw/workspace/micro-office`.
- Started the project with Docker Compose; frontend runs at `http://localhost/`, backend at `http://localhost:8080/`.
- Added a portal API reference doc at `micro-office/docs/portal-api-reference.md` for the three canonical portal read APIs:
  - `GET /api/portal/users/{id}`
  - `GET /api/portal/objects/{id}`
  - `GET /api/portal/products/{id}`
- For external-object customer data, finalized the field semantics as:
  - `customerRole` = 客户属性
  - `customerScale` = 客户类型
- Customer 属性/类型 are now stored as raw imported values from Excel, with no backend enum mapping or fixed-value restriction.
- Updated `micro-office/import_customers.js` so future customer imports store raw values directly from Excel.
- Added/updated `micro-office/scripts/backfill-customer-role-scale.js` to backfill existing customer records from Excel; it supports dry-run by default and `--apply` for writes.
- Refilled existing customer data from `C:\Users\user\Downloads\基础信息-部分.xlsx` / sheet `东华-杭州1彭和春组客户信息`; final backfill updated 2749 customer names / 2767 DB rows.
- Updated frontend labels for customer external objects and object portal views so the UI now shows `属性` and `类型` instead of `角色` and `规模`.
- Verified the final state by rebuilding services and running `micro-office/scripts/api-smoke.mjs`; smoke checks passed after the customer attribute/type changes.
- Relevant commits in workspace git:
  - `a7399fe` `docs: add portal api reference`
  - `820792e` `fix: preserve raw customer role and scale values`
  - `e7a3f80` `fix: rename customer labels to attribute and type`
