# 2026-04-27

- micro-office: 补齐后端全局异常处理，对参数校验失败、约束校验失败、缺少必填参数、非法 JSON/类型不匹配等场景统一返回 `400`，避免这类客户端错误落入通用 `Exception` 处理器后误报 `500`。
- 验证结果：`docker compose up --build -d backend` 成功；`npm run smoke:api` 通过；登录接口错误字段请求从 `500` 修正为 `400`（示例返回：`email 不能为空`）；非法 JSON 返回 `400`。
- 继续优化：登录接口兼容 `login` 字段别名（保留原 `email` 字段）；`LoginRequest` / `RegisterRequest` 的 `@NotBlank` 提示改成更自然的中文；新增 `AuthControllerValidationTest` 回归测试，覆盖 `login` 别名和“登录账号不能为空”校验消息。
- 统一错误响应规范：后端 `ApiResponse` 在保留 `code/message/data` 的同时新增 `errorType/field/errors`，便于前端稳定识别错误类型与字段；登录缺参现可返回结构化错误如 `{"code":400,"message":"登录账号不能为空","errorType":"VALIDATION_ERROR","field":"email","errors":[...]}`；401 也统一带 `errorType: "UNAUTHORIZED"`。前端新增 `frontend/src/api/error.ts` 并在登录/注册页接入，可将服务端字段错误映射到表单项高亮。验证结果：前端 `npm run build` 通过，后端定向测试 `4/4` 通过，`npm run smoke:api` 通过。
- micro-office：将项目中面向用户展示的“外部对象”模块文案统一调整为“客户&对象”，覆盖主菜单、页面标题、门户标题、权限配置页、模板创建提示、关联文案以及对象组织校验错误提示；未调整接口路径、数据表名和内部英文标识。
- micro-office：将“产品服务 / 产品与服务”相关前端展示文案统一调整为“产品&服务”，覆盖主菜单、页面标题和权限配置页；前端容器已重建并确认运行中的静态包包含新文案。
- micro-office：按“入口移除 + 旧链接回退”的方式删除人员管理模块中的门户功能：人员列表移除“门户”按钮，`/users/:id/portal` 与详情路由改为重定向回 `/users`，布局标题中不再保留“人员门户/人员门户详情”映射；前端构建通过并已重建 frontend 容器验证生效。
