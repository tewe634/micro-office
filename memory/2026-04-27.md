# 2026-04-27

- micro-office: 补齐后端全局异常处理，对参数校验失败、约束校验失败、缺少必填参数、非法 JSON/类型不匹配等场景统一返回 `400`，避免这类客户端错误落入通用 `Exception` 处理器后误报 `500`。
- 验证结果：`docker compose up --build -d backend` 成功；`npm run smoke:api` 通过；登录接口错误字段请求从 `500` 修正为 `400`（示例返回：`email 不能为空`）；非法 JSON 返回 `400`。
