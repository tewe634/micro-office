- Durable preference/context from 2026-03-21: for `micro-office` portal design, user does **not** want CRM-style “customer health cockpit” pages. Preferred direction is aggregation portals centered on performance/work data: product portals aggregate which salespeople sold to which customers and how much; customer portals aggregate which salespeople generated how much performance plus related performance/work items; other external-object portals aggregate related work; user portals vary by role (sales = performance + related work/customers/products, other roles = work summary + related work).
- Implemented first mock-data portal version in `micro-office` matching that preference. Added backend endpoints `/api/portal/users/{id}`, `/api/portal/objects/{id}`, `/api/portal/products/{id}`. Added frontend portal routes for users/objects/products and “门户” entry buttons from existing list pages. Portal headers use real entity data; portal bodies use stable deterministic mock aggregates based on entity id.
- Validation status for the portal implementation: frontend `npm run build` passed; backend code compiled/package-built successfully via Docker build (`backend/Dockerfile`) because local `mvn` is unavailable in the environment. Relevant workspace commits: `42a1927` (`feat: add mock portal pages for users objects and products`) and `aca4caa` (`docs: note portal mock implementation progress`).
- Operational note from earlier on 2026-03-21: `micro-office` services could run from existing Docker images, but fresh frontend rebuilds failed under `node:22-alpine` because Vite/Rolldown could not find the musl native binding `@rolldown/binding-linux-x64-musl`.
- On 2026-03-22 this frontend Docker rebuild issue was fixed by changing `micro-office/frontend/Dockerfile` build stage from `node:22-alpine` to `node:22-bookworm-slim` and using `npm ci --include=optional`. Verified successful `docker build` for the frontend image, then `docker compose up -d --build backend frontend` succeeded. `micro-office` frontend/backend are now running with the latest portal mock implementation deployed.
