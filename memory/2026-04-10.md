# 2026-04-10

- 启动了 `micro-office` 的 Docker 开发栈，前端在 `http://localhost`，后端在 `http://localhost:8080`。
- 修复了本地 `micro-office` 启动时的构建问题：后端补上 `PortalTemplateAdminController.firstNonBlank(...)`；前端补上门户模板预览页并修正一个 TypeScript 严格模式报错，随后重新 `docker compose up --build -d` 成功。
- 用户澄清了 `micro-office` 相关沟通期望：在讨论需求时，希望代理先以“产品经理/需求对齐对象”的方式工作——先理解、复述、拆解、补边界、给方案和验收标准，不要默认直接开始实现；只有在用户明确表示“直接做 / 开始实现 / 你去改”时再进入执行开发模式。
- 在 `micro-office` 的产品服务需求里，用户明确了 ABB 品牌下 DP/HP 系列的记录规则：以用户提供图片中的“系列展示口径”原样记录，不强制统一粒度；允许“多个产品合并成一个系列展示”和“一个产品单独作为一个系列展示”并存，先以业务归类/展示规则为准。
- ABB 产品服务结构当前已明确：一级分类共 6 个（DP、HP、SE、电机、低压、成套）。其中 DP 和 HP 按用户提供图片的结构各再分 1 层；SE 下有 4 个二级分类（服务产品、服务业务、电机服务、保内服务）；电机下有 2 个二级分类（高压电机、低压电机）；低压和成套不再继续分层，数据直接汇总到一级分类。
- 已在 `micro-office` 的产品服务模块落地“原始导入分类 + 新产品结构”并行方案：`product` 表新增 `structure_level1 / structure_level2 / series_display_name` 三个字段，产品页新增 ABB 结构说明、编辑项与按设计分类切换的顶部 Tab；用户明确要求设计分类放在上方 Tab 切换，不要作为数据表列展示。门户页同步展示设计结构信息；迁移因仓库已有 `V31` 被顺延为 `V37`，前后端已重新构建并启动成功。
- 用户进一步要求把 DP / HP 的二级目录也直接固化成 Tab；已在 `ProductPage` 中把 DP 二级固定为：标准传动、行业传动、机械传动、伺服与运动控制、PLC&HMI、软启动器、选件与其他；HP 二级固定为：工业传动、高性能机械传动、柜体传动、中压传动、直流传动，并同步到编辑弹窗的二级分类选择。
- `micro-office` 里这轮 ABB 产品服务结构改造已落成 3 个关键提交：`2c061e9`（新增结构字段与门户透出）、`12c52ce`（把设计分类改到顶部 Tab）、`5ae1b24`（固化 DP / HP 二级 Tab）；前端重建后可直接在 `http://localhost` 查看效果。
- 用户追加修正了一条关键口径：DP / HP 的二级 Tab 名称不能由代理自行定义，必须严格以用户上传的结构图原始名称为准；在拿到正确图片前，应撤回主观定义的 DP / HP 二级 Tab，避免页面出现错误分类口径。
- 用户随后上传了 DP 二级名称图，已按图中原字面把 DP 二级 Tab 固化为：ACS55/150/310/355、ACS180、ACS280、ACS380、ACS380E、ACS510、ACP510、ACM510、ACS530、ACH531、ACQ531、ACS550、ACH550/ACH580/ACQ580、ACS580-01/04 (R0-R11)、ACS580-07、ACS800-11/31、ACS880-01/04(R1-R11)、ACS880-11/31/14/34、Servo (Controller+Driver+Motor)、AC500/AC500-eco/HMI、External Options/DP Others；HP 仍待用户提供图片后再按原字面固化。
- 用户之后又上传了 HP 二级名称图，已按图中原字面把 HP 二级 Tab 固化为：ACS580MV、ACS800-67、ACS800-67 LC、ACS800-77 LC、ACS800-87 LC、ACS800/860/880 MD(Module&Cabinet)、ACS800/880-07/ACS880-07C/ACS880-07XT、ACS800/880-14/04(n*R8i)/ACS880-04XT/HES880、ACS800/ACS880-17/37/SD-LC、ACS880-87 LC、ACS1000/ACS2000/5000A、ACS1000Ex/ACS2000Ex、ACS5000W/6080/LCI、LCI、DC Drive products、HPD Others(Options and Packages)、Packaging、PCS6000 Wind、Wind Service、Windmill。
