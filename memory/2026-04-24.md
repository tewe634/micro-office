# 2026-04-24

- micro-office: 线上 `BI数据分析师门户模板` 预览接口报错，根因是非 CEO 模板引用了 `ceo.todo_list` 块；已在线上临时禁用该块后恢复 `preview` 200。
- 本地已补根因修复：
  - 后端 `PortalTemplateAdminController` 在保存模板分区时，禁止**启用中的**非 CEO 模板引用 `ceo.*` 数据块。
  - 前端 `AdminPortalTemplateEditorPage` 过滤 CEO 专用块、展示兼容性告警，并阻止重新启用不兼容块。
- 验证结果：前端 `npm run build` 成功；后端 `docker compose build backend` 编译成功。
- Git 提交：`2a57b90d` (`fix: block CEO portal cards on incompatible templates`).
- 线上只读扫描：ACTIVE 的 CEO 数据块共 7 个，其中 `ceo.todo_list` 仍有 30 个引用，按模板 code 粗略看有 29 个非 CEO 模板仍在引用，后续建议继续批量清理。
