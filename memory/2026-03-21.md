- Started `micro-office` services via Docker Compose using existing images: `docker compose up -d backend frontend`. Verified frontend responds on `http://127.0.0.1/` (HTTP 200) and backend responds on `http://127.0.0.1:8080/` (HTTP 403 at root, indicating the app is up behind auth/security).
- Attempting `docker compose up -d --build backend frontend` failed at the **frontend** image build stage. The failure is from Vite/Rolldown missing native binding `@rolldown/binding-linux-x64-musl` inside the `node:22-alpine` build container. Existing cached frontend/backend images still start successfully.
