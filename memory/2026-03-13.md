# 2026-03-13 工作记录

## 微办公论坛软件 (micro-office)

### 项目位置
`/home/user/.openclaw/workspace/micro-office/`

### 当前状态：四个服务全部运行中 ✅
- PostgreSQL 16 → localhost:5432
- Redis 7 → localhost:6379
- 后端 Spring Boot → localhost:8080
- 前端 Nginx → localhost:80

### 已验证通过的接口
- 用户注册/登录（JWT）
- 创建组织
- 创建工作主帖
- 创建工作节点
- 添加评论
- 工作台（todo/active/done 视图）
- 前端页面 HTTP 200

### 修复过的问题
1. Docker Hub 不通 → 配了国内镜像源（/etc/docker/daemon.json）
2. Maven Central 慢 → Dockerfile 里加了阿里云 Maven 镜像
3. TIMESTAMPTZ ↔ LocalDateTime 不匹配 → 实体改用 OffsetDateTime
4. PG 自定义 ENUM 类型 → JDBC URL 加 stringtype=unspecified
5. 工作台 view 参数缺默认值 → 加了 defaultValue="todo"

### 技术栈
- 后端：Java 17 + Spring Boot 3.3 + MyBatis-Plus + PostgreSQL + Redis + JWT + Flyway
- 前端：React 18 + TypeScript + Vite + Ant Design 5
- 部署：Docker Compose（四个容器）

### 待做
- 清理旧的 Node.js 后端文件（src/index.ts, prisma/, tsconfig.json 等仍在 backend/ 下）
- GlobalExceptionHandler 加异常日志打印（当前吞异常不打堆栈）
- 更多接口测试（打卡、任务池领取、流程回退等）
- 需求文档保存到项目文件
- 前端代码拆分（Vite 警告 chunk > 500KB，需 dynamic import）

### sudo 密码
donghua110

---

## 18:05-18:35 继续工作

### 前端页面全部完成 ✅
之前前端已有完整业务页面（不是空壳），今天确认并补充了：
- 新增 `ThreadListPage.tsx`（工作列表页，含新建工作弹窗）
- 改进 `LoginPage.tsx`（加了注册 Tab，登录/注册切换）
- 更新 `App.tsx` 路由加上 `/threads` 列表路由

### 前端完整页面清单
- 登录/注册页（Tab 切换）
- 工作台（进行中/已完成/待办三视图）
- 工作列表（新建 + 查看详情）
- 工作详情（节点时间线 + 评论区 + 完成节点决策弹窗）
- 组织架构（树形组织 + 岗位表格 CRUD）
- 外部对象管理（按类型筛选 CRUD）
- 产品与服务管理（CRUD）
- 任务池（按岗位查看 + 领取）
- 打卡（上班/下班 + 今日记录）
- 系统管理 - 模块配置（关键词/URL CRUD）
- 系统管理 - 流程模板（模板 + 节点管理）

### Nginx 代理验证
- `http://localhost/api/*` → 后端 8080 代理正常 ✅
- 前端 SPA 路由 fallback 正常

### 项目当前状态：全栈可用
- 浏览器访问 `http://localhost` 即可使用
- 注册 → 登录 → 工作台 → 创建工作 → 节点管理 全流程可走通
- 四个 Docker 容器全部 healthy/running
