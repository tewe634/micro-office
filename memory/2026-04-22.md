# 2026-04-22

- Recovered `micro-office` product classification using Excel source files in `/mnt/d/BaiduNetdiskDownload/` (notably `自主产品.xlsx`, `贴牌产品.xlsx`, `低功率传动与自动化产品 LPDA.xlsx`, `高功率传动产品 HPD.xlsx`, `服务产品 Service.xlsx`, plus ABB/电机/成套/服务 related sheets).
- `frontend/src/pages/product/ProductPage.tsx` was updated to show product classification columns: `structureLevel1`, `structureLevel2`, and `seriesDisplayName`.
- Added/used product reclassification scripts in `micro-office/`:
  - `reimport_product_classification.js`
  - `backfill_product_structure_from_excels.js`
- Both reclassification scripts now support configurable DB connection via env vars: `PRODUCT_DB_HOST`, `PRODUCT_DB_PORT`, `PRODUCT_DB_NAME`, `PRODUCT_DB_USER`, `PRODUCT_DB_PASSWORD`.
- Important product classification rule: `ACS260` products should map under `ABB > DP > ACS280`.
- Relevant commits made in `micro-office`:
  - `17df1a6` — `restore product classification import and visibility`
  - `cf5e79f` — `map ACS260 products under ACS280`
  - `b9eff1e` — `allow product reclass scripts to target configurable DB`
- Local `micro-office` product data was largely repaired; only 4 obsolete `(作废)礼品类` rows remained unclassified after the historical backfill.
- Cloud deploy target is existing `micro-office-prod`; before syncing product data, a remote backup table `product_backup_20260422_remote_sync` was created. Frontend image was redeployed on the cloud host, and a DB backfill to the remote product database was started through an SSH tunnel (`localhost:6543` -> remote `5433`). Completion status of that remote backfill was not confirmed before compaction.
- Synced the current `micro-office` codebase to `/opt/micro-office-prod/project/micro-office/` on `47.111.167.198` via rsync and kicked off a direct front/back overwrite deploy with `docker compose -p micro-office-prod -f docker-compose.minimal.yml up -d --build backend frontend`.
- The cloud build initially failed because `eclipse-temurin:17-jre` could not be fetched from Docker Hub; pulled DaoCloud mirror images and tagged them locally on the server to continue the build.
- The SSH control/tunnel session later expired, so the in-progress remote backfill process and direct container inspection could not be rechecked from shell. External verification still showed the site root on port 80 responding `200`, and `/api/products?page=1&size=1` through nginx returned `401 未登录或登录已失效`, indicating frontend nginx and backend API proxy were both up after the overwrite deploy.
- User preference: git commit email should use `2533314379@qq.com`; do not use AI/default-generated emails like `openclaw@local`. Updated both global git config and this workspace repo config accordingly.
- Gitee repo `https://gitee.com/donghua_iot/micro-office.git` was verified synced at `master` commit `93a7e020` (`merge current micro-office fixes onto master`); a follow-up push returned `Everything up-to-date`.
- User also wants git/Gitee to use account `2533314379@qq.com`; Gitee HTTPS auth was configured locally for that account.
- A later request to redeploy the newest local `micro-office` to cloud was blocked because SSH access to `root@47.111.167.198` no longer worked (`Permission denied (publickey,password)`). Future redeploy needs either the server root password or adding the current local SSH public key to the server's `authorized_keys`.
- SSH access to `47.111.167.198` was later restored with a root password provided in chat (do not store the secret). The latest local `micro-office` code was rsynced again to `/opt/micro-office-prod/project/micro-office/`, then `docker compose -p micro-office-prod -f docker-compose.minimal.yml up -d --build backend frontend` was rerun successfully using cached/mirrored base images. Verification after startup: site root returned `200`, `/api/products?page=1&size=1` returned normal unauthenticated `401`, and both `micro-office-prod-backend-1` and `micro-office-prod-frontend-1` were up.
