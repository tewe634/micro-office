# 2026-03-23

- 启动了 `micro-office` 的前后端服务（Docker Compose）。
- 排查“删除测试新增组织/产品时报后端 400”问题：根因是外键关联未清理。
  - 组织删除被 `sys_user.org_id` 引用拦住。
  - 产品删除被 `work_thread.product_id` 引用拦住。
- 已清理 2026-03-22 产生的一批测试残留数据：测试组织、岗位、用户、产品、客户对象、主工作流、子工作流及其节点/评论/引用。
- 已修改 `micro-office` 后端删除逻辑：组织/产品删除前先做依赖检查，返回中文可读错误，而不是直接抛数据库外键异常；并给全局异常处理加了关联数据兜底提示。
- 按“元数据管理后台”定位继续收缩后端：删除工作流 / 模板 / 打卡 / 仪表盘相关后端控制器、服务、实体、Mapper、DTO、枚举；新增 `V20__remove_workflow_and_unused_modules.sql` 删除对应数据库表和菜单权限残留，并验证组织、人员、产品、外部对象、权限接口仍正常。
- 已确认 `micro-office` 的产品定位调整为：只保留组织架构、人员、产品、外部对象及系统权限配置；工作流相关后端表和模块属于应删除范围。相关提交：`9fbe179`（改进组织/产品删除报错）、`8b79ebb`（移除工作流和无用后端模块）。
- 已为 `micro-office` 实现持久登录与鉴权守卫：后端新增 `auth_session` 会话表和 `/api/auth/logout`，JWT 默认不带 `exp`、仅在手动退出时撤销当前会话；未登录/失效 token 统一返回 401。前端新增启动期鉴权校验、401 自动清理登录态并跳转 `/login`、手动退出先调用后端注销接口。相关迁移：`V21__auth_session.sql`。
