# 2026-03-23

- 启动了 `micro-office` 的前后端服务（Docker Compose）。
- 排查“删除测试新增组织/产品时报后端 400”问题：根因是外键关联未清理。
  - 组织删除被 `sys_user.org_id` 引用拦住。
  - 产品删除被 `work_thread.product_id` 引用拦住。
- 已清理 2026-03-22 产生的一批测试残留数据：测试组织、岗位、用户、产品、客户对象、主工作流、子工作流及其节点/评论/引用。
- 已修改 `micro-office` 后端删除逻辑：组织/产品删除前先做依赖检查，返回中文可读错误，而不是直接抛数据库外键异常；并给全局异常处理加了关联数据兜底提示。
