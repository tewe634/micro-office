- Worked in `micro-office` on frontend layout fixes for the 人员管理 module only (`frontend/src/pages/user/UserTab.tsx` and `frontend/src/pages/user/PositionTab.tsx`). User explicitly wanted other modules left alone.
- Final UX direction for 人员管理: fixed-height list area, vertical scrolling for rows, fixed pagination area below, and consistent spacing/height between the 人员 and 岗位 tables. Repeatedly adjusted bottom clearance so the last row is not hidden by the pager.
- Relevant commits made during this iteration:
  - `860f95e` — fix(frontend): limit user management tables to vertical scrolling
  - `d3973de` — fix(frontend): keep last rows clear of user table pager
  - `a3bc477` — fix(frontend): add bottom clearance to user management tables
  - `aa671d9` — fix(frontend): increase bottom clearance for user tables
  - `b1ab926` — fix(frontend): increase bottom clearance for position table
  - `6fb660f` — fix(frontend): align position table spacing with user table
- Earlier in the same session, broader frontend viewport/layout refactors were also made in `micro-office/frontend` and committed, including:
  - `58409b6` — keep data pages within viewport
  - `a5c2152` — restore table scroll and footer layout
  - `a2d32d1` — keep user table wide with horizontal scroll
- Follow-up fix for 岗位分页 coupling:
  - split positions API into `GET /api/positions` (full list for dropdowns/permissions) and `GET /api/positions/page` (paged data for 人员管理 → 岗位 tab pager)
  - rewired frontend `positionApi` / `PositionTab` / user-position lookup loads accordingly
  - validated with Docker builds for backend/frontend, then recreated `backend` and `frontend` containers
  - commit: `2982533` — `fix(users): wire position pager to dedicated backend API`
- Root cause of "still old effect" after frontend rebuild:
  - frontend container was already serving the new bundle (`/positions/page` present in built JS)
  - backend was missing MyBatis-Plus `PaginationInnerInterceptor`, so `selectPage(...)` did not apply physical pagination/total handling as expected
  - added `backend/src/main/java/com/microoffice/config/MybatisPlusConfig.java`, rebuilt and restarted backend
  - commit: `b277493` — `fix(backend): enable mybatis-plus pagination`
- Product page follow-up UI alignment:
  - updated `frontend/src/pages/product/ProductPage.tsx` to match 人员管理 module structure more closely
  - removed `FixedTablePage` usage on this page and switched to the same pattern of top filters, fixed-height table area, table-only scrolling, and fixed footer pagination
  - rebuilt frontend and recreated the frontend container
  - commit: `07795fb` — `fix(products): align table scrolling with user management`
- Unified management-list layout after spacing complaints on 产品服务 / 外部对象:
  - switched `UserTab`, `PositionTab`, `ObjectPage` (`ObjectTable`), and `ProductPage` to the shared `FixedTablePage` container
  - removed page-specific hard-coded table height math (`calc(100dvh - 455px)`) from these list pages in favor of shared flex layout with `scroll={{ y: '100%' }}`
  - goal: make 人员管理、岗位、外部对象、产品服务 list pages visually consistent and reduce oversized blank space before pagination
  - rebuilt frontend and recreated the frontend container
  - commit: `60cadd3` — `refactor(frontend): unify list layout across management pages`
- User then requested rollback of the previous step:
  - restored `UserTab`, `PositionTab`, `ObjectPage`, and `ProductPage` to their pre-`60cadd3` versions from `HEAD^`
  - rebuilt frontend and recreated the frontend container
  - commit: `f1e2d67` — `revert(frontend): undo unified management list layout`
- Product/permission decision clarified by user:
  - the 组织架构 module in `micro-office` should be fully open for viewing the company-wide org chart
  - this visibility rule should not conflict with, or be restricted by, 人员管理 permission controls
  - practical interpretation: org-chart browsing is open/read-only, while personnel-management permissions still govern user/position management operations
- Implemented and deployed the org-chart/personnel permission split in `micro-office`:
  - backend `OrgController.chart()` now returns the full company org-chart user set instead of filtering by personnel data scope
  - added `MenuPermissionService` and enforced `/users` module permission on user/position list/detail/mutation APIs, so 人员管理 is no longer implicitly open to everyone
  - frontend now treats only `/org` as always-visible; `/users` is restored to permission-driven navigation and route guarding
  - admin permission page now configures 人员管理 again, while clearly documenting that 组织架构 remains globally visible
  - added Flyway migration `V19__restore_users_menu_permissions.sql` to restore default `/users` access for `ADMIN` and `HR`
  - rebuilt and redeployed `backend` + `frontend` via Docker Compose; verified with authenticated API checks that SALES receives `403` on `/api/users` while still seeing the full `/api/orgs/chart` dataset (83 users at deployment time)
  - commit: `258d324` — `feat(permissions): decouple org chart access from personnel module`
- Follow-up org-chart display correction from user feedback:
  - current org data under `商务部` is `公共组` / `商务一组` / `商务二组`
  - user clarified that 商务专员 should count as the leader of their own group, not of the whole `商务部`
  - adjusted `frontend/src/pages/org/OrgPage.tsx` so the “商务专员 => 负责人” special rule applies only to direct child groups under `商务部`, while `商务部` itself falls back to its own leader-marked人员（如 `商务经理`）
  - rebuilt and redeployed frontend via Docker Compose after the fix
  - commit: `cd0d079` — `fix(org): scope business specialist leaders to their groups`
