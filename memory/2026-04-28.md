# 2026-04-28

- 启动了 `micro-office` 的 Docker 前后端服务，并确认前端首页 200、后端 API smoke 通过。
- 与用户确认“人员、组织、岗位合并”先走 A 方案：只合并入口和页面交互，不改后端数据模型。
- 已完成前端实现：`/org` 统一承载组织/人员/岗位；`/users` 兼容跳转到 `/org?tab=users`；组织节点可跳到对应组织的人员筛选。
- 已在 workspace 提交代码：`bd11b103 feat(micro-office): merge org users and positions entry`。
- 将“组织架构”菜单文案改为“人员&组织”，并提交：`d57c7844 chore(micro-office): rename org menu label`。
- 为 `micro-office` 的岗位增加 `sortOrder` 字段和列表排序：前端岗位页可编辑/展示排序，后端岗位列表按 `sortOrder -> code -> id` 返回；已重建 Docker 前后端并通过 API smoke。
- 这次发现项目 README 写了 Flyway，但当前后端依赖里没有自动 migration 能力；`V50__position_sort_order.sql` 已入库，同时当前本地 PostgreSQL 也已手动执行该迁移。
- 已在 workspace 提交代码：`c543fbcf feat(micro-office): add position sort order`。
- 已按用户确认的规则调整 `micro-office` 组织视角下的人员归集：总经办只显示 5 个 `ADMIN`；其他组织按“本节点 + 全部下级组织”聚合；并额外映射王舟珍（管理体系/财务部负责人）、杨筱辉（业务数字化负责人）、方俊锋（生产成套部负责人）。前后端已重建并通过 API smoke；提交：`62348958 feat(micro-office): align org user visibility rules`。
- 根据用户反馈，进一步收紧 `micro-office` 前端组织图成员展示：总经办、体系层、部门层只显示负责人，不再展开所有成员；小组/城市等更下级节点继续显示成员。前端已重建，API smoke 通过；提交：`8058164f refactor(micro-office): trim org chart member display`。
- 修正了前端组织图上层节点负责人展示逻辑：此前只隐藏了“节点下人员”，但负责人仍按整棵子树聚合，导致体系/部门卡片里仍塞很多人。现改为上层节点只显示本节点直属负责人 + 特殊映射负责人；前端已重建，提交：`6cdd79de fix(micro-office): narrow org chart leaders for upper nodes`。
- 用户反馈组织图上层节点仍然像在展示全部人员；排查发现并非缓存，而是“负责人”区仍按整棵子树的 leaderCandidate 聚合。已修正为：总经办/体系/部门层只展示本节点直属负责人 + 特殊映射负责人，不再把下级组织负责人全部堆上来；前端已重建并更新到新资源 `index-MHt3DZu_.js`；提交：`6cdd79de fix(micro-office): narrow org chart leaders for upper nodes`。
- 用户明确总经办前端卡片只保留杨筱辉作为负责人，但底层关联关系仍保留 5 个管理员供工作流平台和组织规范化查找使用。已调整前端展示并更新到资源 `index-D-0ONpl-.js`；提交：`68b40a2e fix(micro-office): show single root org leader`。
- 根据用户指定，进一步把前端组织图“总经办”卡片收窄为只展示 `杨筱辉` 1 位负责人；底层 5 个管理员关联关系和工作流查找逻辑保持不变。前端已重建并切到新资源 `index-D-0ONpl-.js`；代码提交：`68b40a2e fix(micro-office): show single root org leader`。
- 根据最新口径调整 `micro-office` 前端组织图展示：总经办仍只显示杨筱辉；产品支持体系与销售体系负责人固定为杨筱辉；体系节点继续收起成员，但其下部门节点恢复为“负责人 + 部门人员”原展示。前端已重建并切到新资源 `index-D_Q8mynR.js`；提交：`3c8272ec fix(micro-office): restore department member display`。
- 用户补充前端展示口径：管理体系下的商务部负责人应为吴敏，且商务部人员明细不在商务部卡片平铺，而是在其下级部门展示。已调整前端显示规则并发布新资源 `index-Bs9OS-5y.js`；代码提交：`8aec9bda fix(micro-office): tune business department org card display`。
