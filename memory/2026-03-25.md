# 2026-03-25

- 启动了 micro-office 前后端服务，并确认前端 `http://127.0.0.1/` 与后端 `http://127.0.0.1:8080` 可用。
- 修复了 micro-office 人员管理 -> 人员门户 的岗位切换前端问题：切换过程中会追踪 pending 岗位、禁用重复切换，并将主岗位切回无 query 参数的默认态，避免展示与实际数据错位。提交：`d189333` (`fix: stabilize user portal position switching`)。
- 继续复查后定位到真正卡住切换的根因：门户当前岗位匹配逻辑把 `activePortal`、`primaryPositionId`、URL 参数混成集合后按选项顺序命中，导致总优先命中主岗位。已改为按候选优先级顺序匹配。提交：`e69f2eb` (`fix: honor active portal selection priority`)。
- 为 micro-office 新增了“销售协同配置”模块：后端新增销售协同模板/部门绑定/部门覆盖/预览 API，并新增 `sales_collab_*` 配置表与协同组/场景初始化；前端在系统管理中新增了模板管理、部门应用和责任人预览页面。已通过前端构建，并通过后端容器构建、Flyway 迁移与接口联调验证。
- 销售协同配置模块功能提交为 `6dfb4df`（`feat: add sales collaboration configuration module`）；迁移文件为 `V22__sales_collab_config.sql`，新增表：`sales_collab_group`、`sales_collab_scene`、`sales_collab_group_scene`、`sales_collab_template`、`sales_collab_template_rule`、`sales_collab_org_binding`、`sales_collab_org_rule`。
- 继续补齐销售协同配置：增加了模板复制、部门配置复制，以及“领导”来源类型解析；前端已加入复制入口和领导规则配置，前后端容器都已重建，并通过临时模板联调验证复制与领导解析链路可用。
