# 2026-03-27

- 已启动 `micro-office` 项目前后端 Docker 服务：前端 `http://localhost`，后端 `http://localhost:8080`，PostgreSQL/Redis 也已正常运行。
- 用户说明了下一步需求：人员管理模块中的“个人门户视图”需要调整模板和展示信息；稍后会提供手绘草图，届时按草图修改布局信息。
- 已先定位相关代码：前端主要在 `micro-office/frontend/src/pages/portal/PortalPage.tsx`，人员管理入口在 `micro-office/frontend/src/pages/user/UserTab.tsx` / `UserPage.tsx`，后端数据聚合在 `micro-office/backend/src/main/java/com/microoffice/controller/PortalController.java`。
- 已完成 `micro-office` 人员管理模块“个人门户视图”前后端改版：整体风格改为更接近 AI dashboard 页面，顶部为基础信息 hero 区，主体围绕工作流关联状态展开。
- 用户对个人门户的明确偏好：基础信息区不要“当前视图/当前门户”字段，也不要提示文案；岗位切换栏仅在人员拥有多个岗位时显示；只有销售门户（`USER_SALES`）显示第一排按钮型信息块，且这些按钮都应支持下钻查看详情。
- 本次实现去掉了销售门户前端 mock 数据，改为由 `micro-office/backend/src/main/java/com/microoffice/controller/PortalController.java` 返回真实结构（如 `salesActionCards`、`workflowStatusCards`、`workBuckets` 等），前端由 `micro-office/frontend/src/pages/portal/PortalPage.tsx` 与 `frontend/src/global.css` 消费并渲染。
- 交付结果已验证：前端 `npm run build` 通过，`docker compose up -d --build frontend backend` 已重建并启动；本次功能提交 commit 为 `2ab18ef22b1292c5ead31aa838a150579105b7fa`（`feat: revamp user portal dashboard`）。
- 额外记录：仓库自带 `npm run smoke:api` 在 portal 检查后失败于既有接口 `POST /api/threads -> 404`，看起来不是这次门户改版引入的问题。
