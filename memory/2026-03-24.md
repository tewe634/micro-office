# 2026-03-24

- 按用户请求启动了 `micro-office` 的前后端服务（Docker Compose）。
- 已确认容器状态正常：frontend / backend / postgres / redis 均为 Up。
- 已验证前端首页 `http://127.0.0.1/` 返回 200；后端登录接口 `POST /api/auth/login` 返回 200。
- 备注：后端登录请求体字段名是 `email`，即使使用手机号登录，也需要把手机号放在 `email` 字段里。
- 按用户请求移除了“人员管理”模块人员列表中的“改密码”操作，仅保留“门户 / 编辑 / 删除”；个人修改密码入口仍保留在右上角“设置”。
- 已同步修改 `frontend/src/pages/user/UserTab.tsx`（当前入口）和 `frontend/src/pages/user/UserPage.tsx`（旧页），并重建 frontend 容器使变更生效。
- 落地了 `micro-office` 门户优化：支持一人多岗位门户切换（`positionId`）、客户/产品门户销售范围切换（`scope=personal/department/business/system`），并更新前端门户页 UI 展示岗位/范围切换。
- 已新增测试文档 `docs/multi-position-portal-test-plan.md` 与联调脚本 `scripts/multi-position-smoke.sh`。
- 为验收创建了两个多岗位测试人：一个挂在 `杭州-彭和春`（验证 department 视角），一个挂在 `业务一部`（验证 business 视角）；两者都绑定了 `销售经理 + 主办会计` 两个岗位。
