# 2026-03-14 工作记录

## micro-office 权限系统 + 新模块开发

### 时间线
- 07:52 开始权限管理需求
- ~10:00 完成全部功能

### 完成的功能

#### 1. 角色权限系统 (V2-V5 迁移)
- `sys_user` 新增 `role` 字段：ADMIN / HR / SALES / PURCHASE / FINANCE / STAFF
- JWT token 携带 role claim，JwtFilter 解析为 Spring Security authorities
- SecurityConfig 按角色限制 API 端点访问（/api/orgs, /api/users → HR+ADMIN; /api/admin → ADMIN; /api/objects → SALES+PURCHASE+FINANCE+ADMIN）
- `/api/users/me` 公共端点返回用户信息+菜单权限+对象类型权限
- 前端菜单按角色动态显示，路由守卫防 URL 直接访问

#### 2. 灵活权限配置 (V3-V5)
- `role_menu_permission` 表：角色级菜单权限（管理员可配置矩阵）
- `user_menu_permission` 表：个人级菜单权限覆盖（优先于角色默认）
- `position_object_type` 表：岗位级外部对象类型权限
- `user_object_type` 表：个人级对象类型权限覆盖（优先于岗位默认）
- 前端权限配置页面3个Tab：角色模块权限 / 人员权限（模块+对象类型） / 岗位对象类型

#### 3. 外部对象页面改造
- 改为 Tabs 切换，每个对象类型一个 Tab
- 按用户岗位/个人配置过滤可见类型
- 新增 org_id / owner_id 字段，支持归属到组织和负责人

#### 4. 个人门户（/portal）- 新公共模块，菜单最前面
- 基础信息 + 在职天数
- 同岗位排名（按完成工作数）
- 对内关系网（上下级）
- 对外关系网（关联的外部对象）
- 重大事件/奖励记录 CRUD（user_achievement 表，V6）

#### 5. 数据汇总（/dashboard）- 新公共模块，菜单最后面
- 时间维度：今天/昨天/本周/本月，统计工作数/节点数/评论数/打卡
- 组织维度：个人/部门/大区/公司，统计工作数+人员排名+外部对象分布

#### 6. 补齐的后端 CRUD API
- Thread: list(支持状态筛选), update, delete
- Comment: update, delete
- Clock: history(按天数查历史)
- Template: update; TemplateNode: update, delete

### 数据库迁移版本
- V1: 初始 schema
- V2: sys_user.role 字段
- V3: role_menu_permission 表
- V4: user_menu_permission + position_object_type + external_object 归属字段
- V5: user_object_type 表
- V6: user_achievement 表 + portal/dashboard 菜单权限初始化

### 菜单顺序（最新）
个人门户 → 工作台 → 组织架构 → 人员管理 → 外部对象 → 产品服务 → 打卡 → 数据汇总 → 系统管理(子菜单: 权限配置/模块配置/流程模板)
- 已删除：工作列表（合并进工作台）、任务池（整个模块删除）

### 账号信息
- 所有账号密码已统一重置为 `123456`
- 吴益波(id=4): 17363566535@163.com / ADMIN
- 管理员(id=1): admin@test.com / ADMIN
- superadmin(id=18): superadmin@test.com / ADMIN
- 其他用户默认 STAFF 角色

### 权限优先级
- 模块权限：个人配置 > 角色默认
- 对象类型：个人配置 > 岗位默认（主岗+辅岗并集）

---

## 下午 (13:00-14:00) - 工作流大改造 + 测试

### 完成的功能

#### 7. 工作流权限隔离重构 (V7迁移)
- **铁路警察各管一段**：每个节点数据独立，只有节点参与人/创建者/ADMIN/部门领导可查看
- `node_message` 表：节点独立消息通道（文字+文件），只有有权限的人可见
- `node_reference` 表：节点关联引用（可关联其他工作流/外部对象/产品）
- `field_visibility` 表：岗位级字段可见性控制（如技术/仓储看不到销售合同价格）
- `work_thread` 新增 `product_id` 字段
- 新增 `CANCELLED` 状态（ThreadStatus + NodeStatus）
- 部门领导访问权限：BOSS/SALES_DIR/SALES_MGR/DEPT_MGR/SYS_ADMIN 可查看同组织下属节点

#### 8. 工作台改造
- 删除任务池模块（前后端全删）
- 工作列表合并进工作台
- 工作台4个Tab：**待办**（默认）/ 进行中 / 已完成 / 已取消
- 待办Tab显示指派给当前用户但未处理的节点，带红色角标
- 新建工作流支持选择流程模板、关联外部对象、关联产品

#### 9. 工作流详情页改造
- 左侧：工作流基本信息（标题/创建人/关联对象/产品）
- 右侧：流程节点时间线，无权查看的节点显示🔒
- 节点详情抽屉：关联引用管理 + 独立消息通道
- 完成节点时可选：进入下一预设节点 / 指派给具体人 / 自定义新节点 / 标记完成 / 稍后决定
- 支持动态添加节点、重新指派处理人

#### 10. 数据汇总简化
- 移除顶层"时间维度/组织维度"两个Tab
- 直接以组织维度Tabs为主，时间筛选作为Tab内过滤条件
- 组织Tabs根据岗位自动生成（销售员只看个人，经理看部门，总监看含下级，老板看全公司）

### 数据库迁移
- V7: node_message + node_reference + field_visibility + work_thread.product_id + CANCELLED状态

### 岗位→组织可见范围
| 岗位code | 可见Tabs |
|----------|----------|
| SALES_REP/BIZ_SPEC/PURCHASE_SPEC/WAREHOUSE_SPEC/ACCOUNTANT/HR_SPEC/SUPPORT_SPEC | 个人 |
| SALES_MGR/DEPT_MGR | 个人 + 本部门 |
| SALES_DIR | 个人 + 本部门 + 含下级 + 上级含下级 |
| BOSS/SYS_ADMIN | 个人 + 本组织 + 含下级 + 公司 |

### 端到端测试结果 (13:45)
- 26项通过，0项失败
- 已修复：韩丹丽(id=13)角色从STAFF改为HR
- 已知：节点权限拒绝返回code:400而非403（功能正确，不影响使用）

### 删除的文件
- `frontend/src/pages/taskpool/TaskPoolPage.tsx`
- `frontend/src/pages/thread/ThreadListPage.tsx`

### Git commits
- `0977e0f` 添加角色权限管理系统
- `9710ce3` 添加角色权限配置页面
- `5822dea` 细化权限系统：个人模块权限+岗位对象类型+外部对象归属+Tabs切换
- `653203e` 数据汇总组织维度改为自动关联Tabs
- `bbb74bf` 数据汇总移除顶层时间/组织Tab
- `cd9d583` 工作台新增待办Tab+工作流大改造+删除任务池
