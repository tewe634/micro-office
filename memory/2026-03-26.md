# 2026-03-26

- 在 `micro-office` 项目中启动并重建了前后端 Docker 服务；可通过 `http://localhost` 访问前端、`http://localhost:8080` 访问后端。联调时发现后端 `GET /api/admin/field-visibility` 曾返回 500，问题当时未继续排查。
- 修复了“销售协同配置”页面内容超出屏幕时的滚动问题，模板管理/部门应用页签都改为内容区内滚动。提交：`64026f8` (`fix: add scroll support for sales collab config page`)。
- 将销售协同配置中的 `售前技术协同` 与 `售后技术协同` 合并为单一的 `技术协同`（`TECH_COLLAB`），保留 `售前支持` 与 `售后技术支持` 两个场景；前后端和数据库迁移都已同步。提交：`18f8f16` (`feat: merge pre and after sales tech collab`)。
- 进一步收敛销售协同配置：
  - 管理沟通协同只允许按“领导”配置；
  - 领导解析改为围绕“业务销售负责人”自动匹配；
  - 删除模板/规则里的“职责标签”字段；
  - 统一将销售负责人视为主责，其他解析到的人都视为协作，并且预览/解析时会排除销售负责人本人；
  - 新增数据库迁移 `V24__normalize_sales_collab_rule_fields.sql`，把历史 `participant_role` 统一为 `COLLABORATOR` 并清空 `duty_label`。
  提交：`1e0c600` (`feat: simplify sales collab leader config`)。
- 已确认当前数据库中的销售协同迁移版本到 `v24`，且销售协同规则表里已无 `duty_label`，也没有非 `COLLABORATOR` 的历史记录。
- 当前“管理沟通协同”数据库里没有预置模板规则或部门自定义规则，后续需要手工在模板中新增。
- 已复查 `GET /api/admin/field-visibility`：问题仍然存在。用本地默认管理员账号登录后访问该接口会返回 500；进一步对比发现访问不存在的 `/api/admin/not-a-real-endpoint` 也同样返回 500，说明这更像是“缺失路由被全局异常处理误包成 500”，而不是该接口内部业务报错。当前 `AdminController` 里已没有 `field-visibility` 路由，且迁移 `V20__remove_workflow_and_unused_modules.sql` 已删除 `field_visibility` 表；仓库里对该接口的引用目前只在 `scripts/api-smoke.mjs` 和 `README.md` 中，属于陈旧引用。
- 已排查“销售/人员门户修改后刷新看不见效果”：当前运行中的 Docker 镜像仍是 13:01（frontend）/13:07（backend）构建，早于 14:13 的门户改动提交 `5a4b5e1`，且 `docker-compose.yml` 没有源码挂载，所以浏览器刷新只会看到旧 bundle / 旧 jar。尝试 `docker compose up -d --build frontend backend` 时又发现后端源码当前无法编译：`PortalController.java` 在销售人员门户分支新增了 `result.put("salesRanking", buildSalesRanking(user, salesRefs, productRefs, seed));`，但该作用域里没有定义 `salesRefs`，Maven 编译报 `cannot find symbol: variable salesRefs`。因此最新门户改动既没有被现网容器加载，也暂时无法直接重建上线。
- 随后已修复该编译阻塞：删除 `PortalController` 销售人员门户分支里那条误加的 `salesRanking` 写入（前端新版销售门户已改为本地 mock ranking，不再依赖后端 `data.salesRanking`），重新执行 `docker compose up -d --build frontend backend` 后前后端镜像均已在 15:18 左右重建并启动成功。额外验证：1）销售人员门户 API 现返回新版摘要卡（含“主推变频器”“待推进工作”以及 `workSummary.todo/inProgress/completed/cancelled`）；2）前端容器 `/usr/share/nginx/html/assets/PortalPage-*.js` 中已包含“销售排名 / 主推变频器 / 杭州恒盈设备 · ASC580 报价跟进”等新版销售门户文案，说明刷新后看到的将是新 bundle。
- 继续收尾：已在 `GlobalExceptionHandler` 新增 `NoResourceFoundException` → 404 处理，重新构建并发布 backend 后，`/api/admin/not-a-real-endpoint` 与已移除的 `/api/admin/field-visibility` 都已从原先错误的 500 改为正常 404（`{"code":404,"message":"接口不存在"}`）。同时复查了一个真实技术岗位人员门户：用户“余乐乐”（技术工程师）当前返回 `variant=USER_WORK_TECH`，摘要卡为“关联工作 / 待办 / 进行中 / 已完成”，`workSummary` 为 `todo/inProgress/completed/cancelled`，前端 bundle 中也确认存在“工作分布 / 关联工作”等新版工作门户文案。
- 已继续清理 `field-visibility` 陈旧引用：删除 `micro-office/README.md` 中对该接口的能力描述，并从 `scripts/api-smoke.mjs` 中移除 `field-visibility` 的 smoke 检查、payload helper 与 cleanup 恢复逻辑；随后全仓 `grep` 已无 `field-visibility` / `FieldVisibility` / `field visibility` 残留，脚本语法检查通过。额外执行 `npm run smoke:api` 时，新的失败点暴露为 `POST /api/threads` 返回 404，说明除了已清掉的 `field-visibility` 外，`api-smoke` 里还存在另一处对旧线程接口的陈旧假设，后续可继续清理。
- 用于说明领导解析的组织示例：`杭州-彭和春 -> 业务一部 -> 销售体系 -> 总经办`。当前数据里：
  - `杭州-彭和春`：彭和春（销售经理）、伊志杰/王忠（销售工程师）；
  - `业务一部`：严春南（副总经理）；
  - `总经办`：杨筱辉（CEO）。
  由此形成的示例逻辑：销售员在“当前部门”下优先匹配彭和春；若业务销售负责人本人就是彭和春，则上提匹配严春南；固定组织选到 `总经办` 时会匹配杨筱辉。
