# V1.1.1 决策收敛记录

本文件已由产品确认，当前无待确认阻塞问题。

## 已确认关键决策

1. 视觉按截图 1:1 还原优先。
2. 范围仅管理端预览页。
3. 调试面板保留且默认折叠。
4. 预览扩展到全部模板类型。
5. 预览主体在编辑页配置，模板级。
6. 主体字段：`entityType`、`entityId`。
7. 存储：`meta.previewEntity`。
8. 不新增表，继续使用现有 4 表。
9. 预览接口：GET + query 参数。
10. 类型不一致：后端拒绝 + 前端预检拦截。
11. 编辑页自动保存：1000ms 防抖。
12. 自动保存失败：toast。
13. 保存中禁止进入预览。
14. 布局策略：宽屏骨架优先，允许横向滚动。
15. 自动轮播延后到后续版本。
