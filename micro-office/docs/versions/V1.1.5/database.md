# V1.1.5 数据库拆解

## 1. 目标

- 本版以写路径隔离为主，不强制新增 schema。

## 2. 必做项

### 2.1 无新增表的实现路径
- 继续使用现有 package 与 node 相关表
- 通过后端服务层和 SQL 路径隔离保证数据边界

### 2.2 审计建议
- 节点保存与 package 保存分别记录审计日志（建议）
- 用于排查“谁改了模板头、谁改了节点拓扑”

## 3. 验收

- 无 schema 变更也可完成本版需求
- 数据层无交叉误写
