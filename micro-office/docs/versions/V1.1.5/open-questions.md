# V1.1.5 待确认/默认决策

## 已默认决策

1. `/packages/{id}/nodes` 若传入 package 字段，后端返回 400（优先方案）。
2. 前端模板包信息区默认改只读（避免误操作）。
3. 节点保存 payload 只允许 `nodes` 顶层键。

## 待确认（若你不改，本版按默认）

1. package 基本信息是否需要独立“保存模板信息”按钮。
2. 400 报错 message 文案是否固定为：`节点保存接口不接受 package 字段`。
3. 是否开启审计日志（记录节点保存与包保存调用人、调用参数摘要）。
